import React from "react";
import styles from './Main.module.css'

const MainPage = () => {
    return (
        <div className={styles.main_container}>
            <h1>Go to the Users page to see all users</h1>
        </div>
    );
};

export default MainPage;